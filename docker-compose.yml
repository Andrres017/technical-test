version: '3'
services:
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: golang-test
    ports:
      - '3308:3306'


#docker-compose up -d
#docker-compose down
